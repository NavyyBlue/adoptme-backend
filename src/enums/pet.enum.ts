export enum PetType {
    Dog = 1,
    Cat,
    Bird,
    Other
}

export enum PetAge {
    Puppy = 1,
    Adult,
    Senior
}

export enum PetColor {
    White = 1,
    Black,
    Brown,
    Gray,
    Mixed    
}